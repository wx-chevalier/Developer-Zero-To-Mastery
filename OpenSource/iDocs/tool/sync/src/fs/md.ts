/**
 * @function 将文件转化为 MarkdownToc
 */
import { FileTree } from './file';

function toMarkdownTocBook(fileTree: FileTree) {
  // 使用首个 header 作为标题，其余作为描述
}

function toMarkdownTocAW(fileTree: FileTree) {
  // 使用首个 header 作为标题，其余作为描述
}
